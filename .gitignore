# --- 密钥：绝不进版本库 -----------------------------------------------------
#     token.txt 是用户放 GitHub PAT 的地方（我读它、用它、用完删掉）。
#     .gh-token* 是同一个用途的备用名字。
token.txt
.gh-token*
*.token

# --- 能重新生成的临时产物 ---------------------------------------------------
_probe/
__pycache__/
*.pyc
*.blend1
tools/probe_run_prefs.txt

# --- 构建产物：zip 是 tools\build_zips.ps1 打出来的，不进版本库 -------------
*_install.zip

# --- 第三方商业插件：永不进版本库 -------------------------------------------
#     ⚠ 这条规则在**公开版**里也必须保留。剥离脚本第一版把它删了，
#       结果 `git add -A` 把 Exoside 那 105 个文件一起提交进了公开分支
#       （216 个文件而不是 111 个）—— 正是授权上最不能出的事。
#       规则留着：目录不在就什么都不匹配，目录在（本地有）就当护栏。
MaterialBakery/QuadRemesher/
